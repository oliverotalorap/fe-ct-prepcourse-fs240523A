function obtenerPrimerStringLargo(array) {
  // Devuelve el primer string con más de 5 caracteres en el array.
  // Tu código:

  const stringLargo = [];
  for(let i = 0; i < array.length; i=i+1){
  stringLargo.push(array.length > 5 ) };
  //stringLargo.push(array[i].length > 5 ) };
  return stringLargo; 

    // for (let i = 0; i < array.length; i++) {
    // if (typeof array[i] === 'string' && array[i].length > 5) {
    // if (array[i].length > 5) {
    //   return array[i]; };  };
 
  // function callback(elemento) {
  //   if (elemento == 5 ) return elemento;
  //   return array.length > 5;  };
}
return obtenerPrimerStringLargo.filter(callback);

module.exports = obtenerPrimerStringLargo;
// NO PASO EXPLICACION
